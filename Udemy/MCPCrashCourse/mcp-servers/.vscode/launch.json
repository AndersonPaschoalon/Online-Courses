{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Server Backend",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}\\shellserver\\main.py",
            "args": [],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        },

    ]
}